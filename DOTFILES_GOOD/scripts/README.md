# `scripts/` — outils propres au bac à sable

Scripts d’aide à la migration (dry-run, diff, génération de snippets) **sans** remplacer `scripts/` à la racine du dépôt.

Quand un script sera prêt, le documenter ici et l’ajouter à `make test-dotfiles-good` si un smoke `sh -n` suffit.
