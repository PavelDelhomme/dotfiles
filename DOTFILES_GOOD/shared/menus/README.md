# Menus (bac à sable)

Réserver ce dossier aux **données** ou **gabarits** de menus (texte, listes d’options, messages) partageables entre shells.

Le **code** interactif (`read`, boucles, couleurs) reste dans les managers POSIX (`core/managers/`) ou dans des scripts sous `scripts/`, pas ici.
