# --- Exemple fin de ~/.zshrc (Zsh uniquement) — ne pas activer sans migration ---
# export DOTFILES_DIR="${DOTFILES_DIR:-$HOME/dotfiles}"
# POSIX commun (bac à sable) :
# test -r "$DOTFILES_DIR/DOTFILES_GOOD/lib/bootstrap_posix.sh" && . "$DOTFILES_DIR/DOTFILES_GOOD/lib/bootstrap_posix.sh"
# OU, tant que la prod utilise l’arbre principal :
# test -r "$HOME/dotfiles/shared/config.sh" && . "$HOME/dotfiles/shared/config.sh"
#
# Zsh spécifique (prompt, complétion, managers chargés en Zsh) :
# test -r "$HOME/dotfiles/zsh/zshrc_custom" && . "$HOME/dotfiles/zsh/zshrc_custom"
