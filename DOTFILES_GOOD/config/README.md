# config/

Modèles ou fragments de configuration **applications** (hors shell) à migrer progressivement depuis l’arbre principal.

Pour la configuration **shell** multi-plateforme, privilégier `../shared/env/` + les entrées minces décrites dans `../snippets/` et `docs/ARCHITECTURE.md`. Les sorties runtime vont plutôt dans `../run/`.
