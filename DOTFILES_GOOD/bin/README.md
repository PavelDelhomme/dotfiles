# `bin/` — wrappers exécutables (futur)

Réservé aux petits exécutables « façade » (ex. `dotfiles-env` qui affiche les variables exportées après bootstrap), **optionnels** et non installés dans `$PATH` tant que tu ne les relies pas explicitement.
